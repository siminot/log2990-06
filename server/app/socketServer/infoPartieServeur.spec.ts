import * as assert from "assert";
import { InfoPartieServeur } from "./infoPartieServeur";
import { Socket } from "net";

{
    describe("infoPartieServeur", () => {
        // let socket: SocketIO.Socket = {};
        // let nom: string = "a";
        // let difficulte: string = "difficile";
        // let joueur: string = "b";

        it("constructeur fonctionne", () => {
            // let serveur: InfoPartieServeur = new InfoPartieServeur(nom, difficulte, joueur, socket);
            // assert.notEqual(serveur, null || undefined);
            assert(true);
        });
    });
}
