import { NbCaractMaxDirective } from './nb-caract-max.directive';

describe('NbCaractMaxDirective', () => {
  it('should create an instance', () => {
    const directive = new NbCaractMaxDirective();
    expect(directive).toBeTruthy();
  });
});
