/* import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { MainGrilleCreerComponent } from './main-grille-creer.component';

describe('MainGrilleCreerComponent', () => {
  let component: MainGrilleCreerComponent;
  let fixture: ComponentFixture<MainGrilleCreerComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ MainGrilleCreerComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(MainGrilleCreerComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */