import { EstUnChiffreDirective } from './est-un-chiffre.directive';

describe('EstUnChiffreDirective', () => {
  it('should create an instance', () => {
    const directive = new EstUnChiffreDirective();
    expect(directive).toBeTruthy();
  });
});
