/* import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { MainGrilleRejoindreComponent } from './main-grille-rejoindre.component';

describe('MainGrilleRejoindreComponent', () => {
  let component: MainGrilleRejoindreComponent;
  let fixture: ComponentFixture<MainGrilleRejoindreComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ MainGrilleRejoindreComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(MainGrilleRejoindreComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */